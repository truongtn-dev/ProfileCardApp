# 📱 Profile Card Viewer (Bài Tập 2)

Dự án demo ứng dụng React Native hiển thị danh sách nhân sự, tập trung vào việc thực hành bố cục giao diện (Layout), quản lý Style và Component.

## 🎯 Mục Tiêu
Dự án này được xây dựng để minh họa các kiến thức cốt lõi:
* **Layout:** Sử dụng Flexbox (`flexDirection`, `justifyContent`, `alignItems`) để chia bố cục.
* **StyleSheet:** Tách biệt style ra khỏi logic code.
* **Props:** Truyền dữ liệu động từ cha xuống component con.
* **Styled-components:** Làm quen với cách viết CSS-in-JS hiện đại.
* **Clean Code:** Tổ chức thư mục dự án gọn gàng, dễ bảo trì.

---

## 🛠 Cài Đặt & Chạy Dự Án

### 1. Yêu cầu môi trường
* Node.js (LTS version)
* Expo Go (trên điện thoại) hoặc Android Emulator/iOS Simulator.

### 2. Cài đặt
Clone dự án hoặc tạo mới, sau đó cài đặt các thư viện cần thiết:

```bash
# Cài đặt các thư viện phụ thuộc (bao gồm styled-components và safe-area)
npm install styled-components react-native-safe-area-context
